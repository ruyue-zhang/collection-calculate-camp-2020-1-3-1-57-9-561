'use strict';

var map_to_three_multiples = function(collections){
  return collections.map(element=>{
    return element * 3; //TODO: 不用写return, 如果只有一行实现
  })
};

module.exports = map_to_three_multiples;
